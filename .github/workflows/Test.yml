name: Test workflow

on:
	push

jobs:
	build:
		runs-on: ubuntu-latest
		steps:
			- uses: action/checkout@v2
			- run: chmod 755 main.cpp Character.cpp Character.h Luke.json Vader.json
			- run: g++ Wall main.cpp Character.cpp