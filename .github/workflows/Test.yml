name: Test workflow

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: chmod +x ./test.sh
      - run: g++ -std=c++17 *.cpp
      - run: ./test.sh