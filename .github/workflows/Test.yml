name: Test workflow

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: g++ -Wall main.cpp Character.cpp
      - run: ./a.out units/Vader.json units/Luke.json units/Yoda.json
      - run: ./test.sh